# The upstream key the proxy spends. The app never sees it again — that is the
# point of a gateway: one place holds the provider credential.
GROQ_API_KEY=gsk-...
# Admin credential for /key/generate. Virtual keys are issued against it.
LITELLM_MASTER_KEY=sk-master-change-me
